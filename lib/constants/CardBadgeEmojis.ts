import { CardBadge } from '../types/CardBadge';

export const CardBadgeEmojis: { [key in CardBadge]: string } = {
    OLD_OWNER: '🕰️',
    FEATURED: '⭐',
    PUBLIC: '🌐',
    HUNDRED_MEMBERS: '👤',
    THOUSAND_MEMBERS: '👥',
};
